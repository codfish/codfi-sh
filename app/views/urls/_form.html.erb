<%= form_for(@url) do |f| %>
  <% if @url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@url.errors.count, "error") %> prohibited this url from being saved:</h2>

      <ul>
      <% @url.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="url-field">
    <%= f.label :full_url, "Shorten your URL!" %>
    <%= f.text_field :full_url %><%= f.submit 'Shorten', :class => "btn btn-primary" %>
  </div>

<% end %>
